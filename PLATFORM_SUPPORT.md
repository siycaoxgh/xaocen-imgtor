# 平台支持说明

## 当前状态

- Windows：主支持平台，`启动.bat` → `webapp.py` 为正式入口。
- macOS/Linux：保留部分平台分支，但尚未完成实机回归，不承诺截图浮层、剪贴板、全局快捷键和系统托盘行为完全可用。

## 入口约定

主界面统一由 `webapp.py` 加载 `ui/` 提供。旧 Tk 窗口和旧兼容启动脚本已移动到 `archive/`，不应再作为新功能入口。

## Windows 已知前置条件

- Python 需要包含 Tkinter。
- pywebview 需要可用的系统 WebView2 环境。
- 截图与动图录制涉及 DPI、多显示器和系统剪贴板，发布前仍需在不同缩放比例下进行实机回归。
